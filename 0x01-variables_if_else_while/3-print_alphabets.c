#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char upper;

	for (upper = 'A'; low <= 'Z'; upper++)
		putchar(upper);
		printf("\n");

	return (0);
}
